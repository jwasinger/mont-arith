package modext

{{ $limbCount := .LimbCount}}
{{ $limbBits := .LimbBits}}

func AddModImpls() []ModArithFunc {
	result := []ModArithFunc {
		{{- range $i := intRange 1 $limbCount}}
			AddMod{{mul $limbCount $limbBits}},
		{{- end}}
	}

	return result
}

func SubModImpls() []ModArithFunc {
	result := []ModArithFunc {
		{{- range $i := intRange 1 $limbCount}}
			SubMod{{mul $limbCount $limbBits}},
		{{- end}}
	}

	return result
}

func MulModMontImpls() []MulModMontFunc {
	result := []MulModMontFunc {
		{{- range $i := intRange 1 $limbCount}}
			MulModMont{{mul $limbCount $limbBits}},
		{{- end}}
	}

	return result
}
